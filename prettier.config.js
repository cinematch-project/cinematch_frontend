//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  trailingComma: "all",
  printWidth: 100,
  singleQuote: false,
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
